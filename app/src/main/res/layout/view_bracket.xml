<?xml version="1.0" encoding="utf-8"?>
<!-- Custom Bracket View for Tournament Knockout Stage Visualization -->
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/spacing_md">

    <!-- Bracket Title -->
    <TextView
        android:id="@+id/tvBracketTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tournament Bracket"
        android:textAppearance="?attr/textAppearanceTitleLarge"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Horizontal ScrollView for bracket stages -->
    <HorizontalScrollView
        android:id="@+id/scrollBracket"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/spacing_md"
        android:scrollbars="horizontal"
        app:layout_constraintTop_toBottomOf="@id/tvBracketTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Container for bracket rounds -->
        <LinearLayout
            android:id="@+id/containerBracketRounds"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- Round 1 (Quarter Finals) -->
            <LinearLayout
                android:id="@+id/roundQuarterFinals"
                android:layout_width="200dp"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_marginEnd="@dimen/spacing_lg"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Quarter Finals"
                    android:textAppearance="?attr/textAppearanceLabelLarge"
                    android:textStyle="bold"
                    android:layout_marginBottom="@dimen/spacing_md"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvQuarterFinals"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:listitem="@layout/item_match_list"
                    tools:itemCount="4"/>
            </LinearLayout>

            <!-- Round 2 (Semi Finals) -->
            <LinearLayout
                android:id="@+id/roundSemiFinals"
                android:layout_width="200dp"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:layout_marginEnd="@dimen/spacing_lg"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Semi Finals"
                    android:textAppearance="?attr/textAppearanceLabelLarge"
                    android:textStyle="bold"
                    android:layout_marginBottom="@dimen/spacing_md"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvSemiFinals"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:listitem="@layout/item_match_list"
                    tools:itemCount="2"/>
            </LinearLayout>

            <!-- Round 3 (Final) -->
            <LinearLayout
                android:id="@+id/roundFinal"
                android:layout_width="200dp"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Final"
                    android:textAppearance="?attr/textAppearanceLabelLarge"
                    android:textStyle="bold"
                    android:layout_marginBottom="@dimen/spacing_md"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rvFinal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    tools:listitem="@layout/item_match_list"
                    tools:itemCount="1"/>

                <!-- Winner Trophy Icon -->
                <ImageView
                    android:id="@+id/ivTrophy"
                    android:layout_width="@dimen/icon_size_xl"
                    android:layout_height="@dimen/icon_size_xl"
                    android:layout_marginTop="@dimen/spacing_lg"
                    android:src="@drawable/ic_trophy"
                    android:contentDescription="@string/trophy"
                    app:tint="?attr/colorPrimary"/>

                <TextView
                    android:id="@+id/tvWinner"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_sm"
                    android:textAppearance="?attr/textAppearanceTitleMedium"
                    android:textStyle="bold"
                    android:textColor="?attr/colorPrimary"
                    tools:text="Winner Team"/>
            </LinearLayout>
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Empty State -->
    <LinearLayout
        android:id="@+id/emptyStateBracket"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:layout_width="@dimen/empty_state_icon_size"
            android:layout_height="@dimen/empty_state_icon_size"
            android:src="@drawable/ic_trophy"
            android:contentDescription="@string/no_bracket"
            app:tint="?attr/colorOnSurfaceVariant"
            android:alpha="0.6"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:text="@string/no_bracket_data"
            android:textAppearance="?attr/textAppearanceBodyLarge"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:alpha="0.6"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
