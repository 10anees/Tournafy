package com.example.tournafy.domain.enums;

/**
 * Enum representing the decision made by the toss winner in cricket.
 */
public enum TossDecision {
    BAT,    // Winner chose to bat first
    BOWL    // Winner chose to bowl first (field)
}
