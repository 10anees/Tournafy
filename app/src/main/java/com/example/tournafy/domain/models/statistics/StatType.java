package com.example.tournafy.domain.models.statistics;

/**
 * Enumeration defining the types of statistics that can be tracked
 * for top performers in Tournaments and Series.
 */

public enum StatType {
    RUNS,
    WICKETS,
    GOALS,
    ASSISTS
}